# Circuit-Diagram-CNN
YOLO convolutional neural network model used to recognize circuit diagram symbols from hand drawn circuit diagrams. It then uses simple netlist building logic to generate netlist for LTspice to ingest for circuit generation.
